{
  "scripts": {
    "start": "npx supabase start",
    "new:function": "npx supabase functions new",
    "serve:function-no-env": "npx supabase functions serve --no-verify-jwt --env-file .env",
    "serve:function": "npx supabase functions serve --env-file .env",
    "deploy:function": "npx supabase functions deploy"
  },
  "devDependencies": {
    "supabase": "^2.22.12"
  }
}
